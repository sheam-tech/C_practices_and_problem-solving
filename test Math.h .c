#include <stdio.h>
#include <math.h>

int main()
{
    double x;
  scanf("%lf",&x);
   // int y=abs(x);
   // double y=sqrt(x);
    double y;
  scanf("%lf",&y);
   // int y=abs(x);
   // double y=sqrt(x);
   double z=pow(x,y);
    printf("%lf",z);

    return 0;
}
